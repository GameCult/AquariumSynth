# Split Notes

`AquariumSynth` was extracted with `git subtree split --prefix=crates/aquarium_synth`.

The Bevy repository still contains its historical in-tree copy so that old
commits remain buildable in context. This repo is the clean future surface for
the synth itself.
